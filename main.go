package main

import "j7go/cmd"

func main() {
	cmd.Execute()
	//t := time.Now().String()
	//timeStr := t[:26]
	//timeStr1 := strings.Replace(strings.Replace(strings.Replace(timeStr, `:`, `-`, -1), `.`, `-`, -1), ` `, `-`, -1)
	//uuuid,_ := uuid.NewUUID()
	//fmt.Println(uuuid.String())
	//fmt.Println(timeStr1+"-"+uuuid.String())

}
